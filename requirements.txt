fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.10,<3.0
python-multipart>=0.0.20,<1.0
psycopg[binary]>=3.2,<4.0  # Treiber fuer PostgreSQL, Version 3 -- DATABASE_URL braucht "postgresql+psycopg://", NICHT "+psycopg2" (siehe .env.example)
jinja2>=3.1,<4.0
reportlab>=4.2,<5.0
pillow>=10.4,<13.0
pypdfium2>=4.30,<5.0
openpyxl>=3.1,<4.0
alembic>=1.13,<2.0
cryptography>=43.0,<51.0
pyotp>=2.9,<3.0
qrcode[pil]>=7.4,<9.0
pytest>=8.0,<10.0
httpx>=0.27,<1.0
tzdata>=2024.1  # IANA-Zeitzonendatenbank fuer zoneinfo (Kalender-Modul Stufe 2, Outlook-Sync) --
# Windows liefert die Datenbank anders als Linux nicht mit dem Betriebssystem mit,
# ZoneInfo("Europe/Berlin") würde dort sonst mit ZoneInfoNotFoundError abbrechen.
